language: objective-c
osx_image: xcode7.2

script: 
- xctool -project Hookah.xcodeproj -scheme Hookah -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6' ONLY_ACTIVE_ARCH=NO clean build test

after_success:
  - bash <(curl -s https://codecov.io/bash)
